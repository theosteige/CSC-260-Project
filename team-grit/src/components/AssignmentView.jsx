import { useState } from 'react'

function AssignmentView({currentAssignment}) {
    const [comments, setComments] = useState(null);
    const [code, setCode] = useState(null);
    const [submission, setSubmission] = useState(null);
}